package com.kodatos.shared.domain.unit

/**
 * Marker interface representing actions that a particular domain unit can consume. Can be extended
 * as per individual requirement, see subclasses of [DomainUnit] for examples
 */
interface Action